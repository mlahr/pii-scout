# App dependencies
# For local dev, also install: pip install -r requirements-models.txt

# Core
numpy<2
pymupdf

# Batch processing
pebble
tqdm

# FastAPI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
pydantic-settings>=2.0

# Ollama (for local LLM)
requests>=2.28.0

# Dictionary-based name detection
marisa-trie>=1.0.0
pybloom-live>=4.0.0

# Testing
pytest>=7.0
httpx>=0.24.0
